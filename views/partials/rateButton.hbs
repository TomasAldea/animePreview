

<form action="{{rate.action}}" method="{{method}}">
  <input type="text" hidden name="_method" value={{restMethod}}>
  <button class="btn btn-outline-primary" type="{{btnText}}">like</button>
  </form>